# RK3506 网关程序 — 内核与文件系统裁剪指南

## 目录

1. [项目背景与硬件概况](#1-项目背景与硬件概况)
2. [内核裁剪 (Kernel Config)](#2-内核裁剪-kernel-config)
3. [设备树裁剪 (Device Tree)](#3-设备树裁剪-device-tree)
4. [文件系统裁剪 (Rootfs / Buildroot)](#4-文件系统裁剪-rootfs--buildroot)
5. [Qt 裁剪](#5-qt-裁剪)
6. [U-Boot 裁剪](#6-u-boot-裁剪)
7. [分区规划建议](#7-分区规划建议)
8. [裁剪后验证清单](#8-裁剪后验证清单)

---

## 1. 项目背景与硬件概况

### 1.1 应用场景

| 项目 | 说明 |
|------|------|
| 芯片 | RK3506 (三核 Cortex-A7 + Cortex-M0) |
| 内存 | 256MB DDR |
| Flash | 256MB (SPI NAND / eMMC) |
| 用途 | 网关：CAN + RS485 数据转发，网口调试 |
| 开发框架 | Qt (界面/逻辑) |
| **不需要** | 蓝牙、WiFi、音频、视频、摄像头、HDMI、GPU加速 |

### 1.2 需要保留的功能

| 功能 | 对应内核/驱动 |
|------|--------------|
| CAN 总线 | SocketCAN, CAN 控制器驱动 |
| RS485/UART | 串口驱动 (ttyS*) |
| 以太网 | MAC 驱动 + PHY 驱动 |
| Qt 运行 | Framebuffer 或 DRM (如有屏) / LinuxFB / 纯逻辑无屏 |
| 基础文件系统 | ext4 / squashfs / ubifs |
| 看门狗 | WDT 驱动 |
| GPIO | GPIO 子系统 (LED指示灯、继电器控制等) |

---

## 2. 内核裁剪 (Kernel Config)

进入内核配置：

```bash
cd kernel
make ARCH=arm rockchip_linux_defconfig  # 或您的 defconfig
make ARCH=arm menuconfig
```

### 2.1 需要关闭的大模块

#### 2.1.1 无线网络 — 全部关闭

```
Networking support --->
    [*] Wireless  ---> 改为 [ ]
        < > cfg80211 - wireless extensions compatibility
        < > Generic IEEE 802.11 Networking Stack (mac80211)

Device Drivers --->
    Network device support --->
        Wireless LAN --->
            全部取消（Realtek, Broadcom, Rockchip WiFi 等全部 < >）
```

**预估节省：内核 1~3MB，rootfs 固件文件 2~10MB**

#### 2.1.2 蓝牙 — 全部关闭

```
Networking support --->
    < > Bluetooth subsystem support  --->
        取消全部

Device Drivers --->
    取消所有 Bluetooth/BT HCI 相关驱动
```

**预估节省：内核 500KB~1MB，rootfs 固件/工具 1~5MB**

#### 2.1.3 音频子系统 — 全部关闭

```
Device Drivers --->
    < > Sound card support --->
        < > Advanced Linux Sound Architecture ---> 全部取消
        < > Open Sound System (DEPRECATED)

    # 同时关闭 Rockchip I2S / PDM 等音频相关驱动
```

**预估节省：内核 500KB~2MB**

#### 2.1.4 GPU / 图形加速 — 关闭

```
Device Drivers --->
    Graphics support --->
        < > Mali GPU support (如果有)
        # 如果网关完全无屏，连 DRM/Framebuffer 都可以关闭
        # 如果有屏幕调试界面，仅保留基础 Framebuffer:
        [*] Support for frame buffer devices --->
            仅保留 Rockchip 的基础 FB 驱动
```

**预估节省：内核 1~5MB，rootfs GPU 库 5~20MB**

#### 2.1.5 视频编解码 / 多媒体 — 全部关闭

```
Device Drivers --->
    < > Multimedia support --->
        < > Media USB Adapters
        < > V4L platform devices  (摄像头等)
        < > Memory-to-memory multimedia devices (硬件编解码)

    # 关闭 Rockchip VPU / RGA / ISP 等：
    < > Rockchip VPU (Video Processing Unit)
    < > Rockchip RGA
    < > Rockchip ISP
```

**预估节省：内核 1~3MB，rootfs 编解码库 5~15MB**

#### 2.1.6 USB 相关 — 按需裁剪

如果不用 USB 外设（U盘、USB转串口等），可以大幅裁剪：

```
Device Drivers --->
    [*] USB support --->
        # 如果完全不用 USB：
        改为 < > USB support
        
        # 如果仅保留基础 USB Host (用于调试/升级)：
        < > USB Mass Storage support     # 如不用U盘
        < > USB Serial Converter support # 如不用USB转串口
        < > USB Gadget Support           # 如不用 USB device 模式
```

#### 2.1.7 文件系统 — 仅保留必需

```
File systems --->
    < > Second extended fs support        # 不用 ext2
    < > The Extended 3 (ext3) filesystem  # 不用 ext3
    [*] The Extended 4 (ext4) filesystem  # 保留 ext4 (如用 eMMC)
    
    # 如用 SPI NAND：
    [*] UBIFS file system support         # 保留
    [*] JFFS2 file system support         # 可选保留
    
    < > Btrfs filesystem support
    < > XFS filesystem support
    < > F2FS filesystem
    < > NTFS file system support
    < > HFS/HFS+ filesystem support
    
    # 网络文件系统 (调试用可保留 NFS，发布版去掉)：
    < > NFS client support   # 发布时关闭
    < > CIFS support
    < > SMB3 support
    
    # CD-ROM/ISO:
    < > ISO 9660 CDROM file system support
    < > UDF file system support
```

#### 2.1.8 输入设备 — 按需裁剪

```
Device Drivers --->
    Input device support --->
        < > Touchscreen interface     # 无触屏则关闭
        < > Joystick interface
        < > Mouse interface           # 无鼠标则关闭
        # 保留基础 keyboard 用于调试
```

#### 2.1.9 其他可关闭项

```
Device Drivers --->
    < > MMC/SD/SDIO card support    # 如果不用 SD 卡 (注意 eMMC 也走此通道)
    < > LED Support                 # 如果不用 LED 子系统 (GPIO 控制的除外)
    < > PWM Support                 # 如果不用 PWM
    < > Industrial I/O support      # IIO ADC 等，如不用
    < > Thermal drivers             # 网关功耗低，可简化
    
Networking support --->
    < > NFC subsystem support
    < > Amateur Radio support
    < > CAN bus subsystem support ---> [*]  # ★ 必须保留！
    < > IrDA subsystem support
    < > RxRPC session sockets

Kernel hacking --->
    # 发布版关闭调试选项
    [ ] Magic SysRq key
    [ ] Debug Filesystem
    [ ] Kernel debugging
    [ ] KGDB: kernel debugger
```

### 2.2 必须保留的内核项

```
# ★ CAN 总线
Networking support --->
    <*> CAN bus subsystem support --->
        <*> Raw CAN Protocol
        <*> Broadcast Manager CAN Protocol
        CAN Device Drivers --->
            <*> Rockchip CAN / CAN FD controller  # 具体看 RK3506 的驱动名

# ★ 串口 (RS485)
Device Drivers --->
    Character devices --->
        Serial drivers --->
            <*> Rockchip serial port support (或 8250/16550)
            # 确保 RS485 模式支持

# ★ 以太网
Device Drivers --->
    Network device support --->
        Ethernet driver support --->
            <*> Rockchip GMAC / EMAC 驱动
            # 对应的 PHY 驱动也要保留

# ★ GPIO
Device Drivers --->
    GPIO Support --->
        <*> Rockchip GPIO support

# ★ 看门狗 (推荐)
Device Drivers --->
    Watchdog Timer Support --->
        <*> Rockchip watchdog

# ★ SPI (如用 SPI NAND)
Device Drivers --->
    SPI support --->
        <*> Rockchip SPI controller
    Memory Technology Device (MTD) support --->
        <*> SPI NAND device support

# ★ I2C (如有 I2C 外设如 EEPROM、RTC 等)
Device Drivers --->
    I2C support --->
        <*> Rockchip I2C adapter

# ★ RTC (实时时钟)
Device Drivers --->
    Real Time Clock --->
        <*> 你使用的 RTC 芯片驱动
```

### 2.3 内核压缩与优化

```
General setup --->
    # 使用 LZ4 或 XZ 压缩内核
    Kernel compression mode (XZ)  # XZ 最小，启动稍慢
    
    [ ] Enable support for printk          # 极限裁剪时可关闭
    [ ] BUG() support                      # 极限裁剪
    [ ] Enable futex support               # Qt 需要！必须保留！
    [*] Optimize for size (-Os)            # ★ 使用 -Os 编译优化

# 关闭模块支持 (全部编入内核，省去 modules 空间)
[*] Enable loadable module support  # 可选关闭，但灵活性降低
```

---

## 3. 设备树裁剪 (Device Tree)

在 `arch/arm/boot/dts/` 下找到 RK3506 的 dts 文件，注释/删除不需要的节点：

```dts
/* 关闭蓝牙 */
&wireless_bluetooth {
    status = "disabled";
};

/* 关闭 WiFi */
&wireless_wlan {
    status = "disabled";
};
&sdio {
    status = "disabled";  /* WiFi 通常走 SDIO */
};

/* 关闭音频 */
&i2s0 {
    status = "disabled";
};
&i2s1 {
    status = "disabled";
};
&pdm {
    status = "disabled";
};
&codec {
    status = "disabled";
};
&sound {
    status = "disabled";
};

/* 关闭 HDMI / 显示输出 (如果无屏) */
&hdmi {
    status = "disabled";
};
&vop {
    status = "disabled";
};
&dsi {
    status = "disabled";
};
&lvds {
    status = "disabled";
};

/* 关闭摄像头 */
&csi2 {
    status = "disabled";
};
&isp {
    status = "disabled";
};

/* 关闭 GPU */
&gpu {
    status = "disabled";
};

/* ★ 保留并正确配置 CAN */
&can0 {
    status = "okay";
    pinctrl-names = "default";
    pinctrl-0 = <&can0_pins>;
};

/* 如有多路 CAN */
&can1 {
    status = "okay";
};

/* ★ 保留并配置 UART (RS485) */
&uart1 {
    status = "okay";
    pinctrl-names = "default";
    pinctrl-0 = <&uart1_xfer &uart1_rts>;  /* RTS 用于 RS485 方向控制 */
    linux,rs485-enabled-at-boot-time;       /* 启动即启用 RS485 模式 */
    rs485-rts-active-low;                   /* 根据硬件设计调整 */
};

/* ★ 保留以太网 */
&gmac {
    status = "okay";
    phy-mode = "rmii";  /* 或 rgmii，根据实际硬件 */
    /* ... PHY 配置 ... */
};

/* ★ 保留看门狗 */
&wdt {
    status = "okay";
};
```

---

## 4. 文件系统裁剪 (Rootfs / Buildroot)

### 4.1 Buildroot 裁剪

如果使用 Buildroot 构建 rootfs：

```bash
cd buildroot
make rockchip_rk3506_defconfig  # 或您的 defconfig
make menuconfig
```

#### 4.1.1 移除的包 (Target packages)

```
Target packages --->
    # ========== 音频/视频 ==========
    Audio and video applications --->
        < > alsa-utils          # 音频工具
        < > alsa-lib            # 音频库
        < > pulseaudio          # 音频服务
        < > gstreamer / gst-plugins  # 多媒体框架
        < > ffmpeg              # 编解码
        < > mpv / mplayer       # 播放器
        < > v4l-utils           # 视频工具

    # ========== 图形/GPU ==========
    Graphic libraries and applications --->
        < > mesa3d              # OpenGL 库 (如果不需要 GPU 加速)
        < > libdrm              # 如果无屏可去掉
        < > wayland             # Wayland 合成器
        < > weston              # Wayland 参考合成器
        < > X.org               # X11 (如不用)
        < > gtk+                # GTK 库
        < > cairo               # 如果 Qt 不依赖
        < > pango               # 如果 Qt 不依赖

    # ========== 蓝牙/WiFi ==========
    Networking applications --->
        < > bluez5-utils        # 蓝牙工具栈
        < > wpa_supplicant      # WiFi WPA
        < > hostapd             # WiFi AP 模式
        < > iw                  # WiFi 配置工具
        < > wireless-tools      # WiFi 工具
        < > NetworkManager      # 网络管理器 (用不上, 手动配置即可)
        < > connman             # 连接管理器

    # ========== 不需要的工具 ==========
    Shell and utilities --->
        < > bash                # 用 busybox ash 代替即可
    
    Text editors and viewers --->
        < > vim                 # 用 busybox vi 代替
        < > nano
    
    System tools --->
        < > systemd             # 如果可以用 busybox init / sysvinit
        < > docker              # 容器
        < > lxc                 # 容器
    
    Debugging, profiling --->
        < > strace              # 发布版可去
        < > gdb / gdbserver     # 发布版可去
        < > valgrind            # 发布版可去
        < > ltrace

    # ========== 语言运行时 ==========
    Interpreter languages --->
        < > python / python3    # 如果不需要
        < > nodejs
        < > lua
        < > ruby
        < > perl
```

#### 4.1.2 保留的包

```
Target packages --->
    # ★ 网络工具 (按需)
    Networking applications --->
        [*] busybox 内置的网络工具 (ifconfig, route, ip)
        [*] iptables            # 如需防火墙
        [*] openssh / dropbear  # 远程调试 (发布可去掉 SSH)
        [*] can-utils           # ★ CAN 调试工具 (candump, cansend 等)

    # ★ 串口工具
    Hardware handling --->
        [*] picocom 或 minicom  # 串口调试 (发布可去掉)

    # ★ Qt 相关 (见第5节详细说明)
    Graphic libraries --->
        [*] Qt5 ---> (仅选需要的模块)
```

### 4.2 手动清理 rootfs 中的大文件

构建完成后检查 rootfs，清理不需要的文件：

```bash
# 找到占空间最大的目录
du -sh rootfs/* | sort -rh | head -20

# 常见可删除项：
rm -rf rootfs/usr/share/doc/          # 文档
rm -rf rootfs/usr/share/man/          # man 手册
rm -rf rootfs/usr/share/info/         # info 文档
rm -rf rootfs/usr/share/locale/       # 仅保留 zh_CN 和 en_US
rm -rf rootfs/usr/share/zoneinfo/     # 仅保留需要的时区
rm -rf rootfs/usr/include/            # 头文件 (运行时不需要)
rm -rf rootfs/usr/lib/pkgconfig/      # pkg-config 文件
rm -rf rootfs/usr/share/fonts/        # 不需要的字体 (Qt 自带字体除外)

# strip 所有二进制和库文件
arm-linux-gnueabihf-strip rootfs/usr/lib/*.so*
arm-linux-gnueabihf-strip rootfs/usr/bin/*
arm-linux-gnueabihf-strip rootfs/bin/*
arm-linux-gnueabihf-strip rootfs/sbin/*
```

### 4.3 Buildroot 自动优化选项

```
Build options --->
    [*] strip target binaries           # ★ 自动 strip
    [*] gcc optimization level (optimize for size (-Os))
    [ ] enable compiler cache (ccache)  # 编译缓存，不影响产物大小
    
System configuration --->
    /dev management (Dynamic using devtmpfs only)  # 不用 udev/eudev
    Init system (BusyBox)               # 用 BusyBox init 代替 systemd
    [ ] Enable root login with password # 如需安全可关闭
    [ ] install timezone info           # 关闭以节省空间
```

---

## 5. Qt 裁剪

Qt 是 rootfs 中最大的组件之一，精细裁剪非常关键。

### 5.1 Qt 模块选择

在 Buildroot 中配置 Qt5：

```
Target packages --->
    Graphic libraries and applications --->
        [*] Qt5 --->
            # ★ 保留 (网关必需)
            [*] qt5base              # 核心库
            [*]   Concurrent module  # 多线程 (如需要)
            [*]   Network module     # ★ 网络功能
            [*]   SQL module         # 如需数据库记录
            [*]     SQLite support   # 轻量数据库
            [*]   Serial Port module # ★ Qt Serial Port (RS485)
            [*]   Serial Bus module  # ★ Qt Serial Bus (CAN)
            
            # ★ 如果有屏幕 (用 Qt GUI):
            [*]   GUI module
            [*]   Widgets module
            [*]   linuxfb backend    # LinuxFB 渲染
            [ ]   eglfs backend      # 不需要 GPU, 关闭
            [ ]   OpenGL support     # 关闭 GPU 加速
            
            # ★ 如果纯后台无屏:
            [ ]   GUI module         # 关闭 GUI
            [ ]   Widgets module     # 关闭 Widgets
            
            # ========== 不需要的模块，全部关闭 ==========
            [ ] qt5multimedia        # 多媒体
            [ ] qt5webengine         # 浏览器引擎 (巨大! 100MB+)
            [ ] qt5webkit            # 旧浏览器引擎
            [ ] qt5webview           # WebView
            [ ] qt5webchannel
            [ ] qt5websockets        # 如不用 WebSocket 可关闭
            [ ] qt5bluetooth         # ★ 蓝牙模块，关闭
            [ ] qt5nfc               # NFC
            [ ] qt5location          # 定位
            [ ] qt5sensors           # 传感器
            [ ] qt5multimedia        # 多媒体
            [ ] qt5graphicaleffects  # 图形特效
            [ ] qt5imageformats      # 额外图片格式
            [ ] qt5svg               # SVG (如不用)
            [ ] qt5charts            # 图表 (如不用)
            [ ] qt5datavis3d         # 3D 数据可视化
            [ ] qt53d                # 3D 模块
            [ ] qt5virtualkeyboard   # 虚拟键盘
            [ ] qt5wayland           # Wayland 支持
            [ ] qt5x11extras         # X11 支持
            [ ] qt5tools             # Qt 工具
            [ ] qt5doc               # Qt 文档
            [ ] qt5translations      # Qt 翻译文件 (节省几MB)
            [ ] qt5quickcontrols     # QML Controls
            [ ] qt5quickcontrols2    # QML Controls 2
            [ ] qt5quick             # QML 引擎 (如不用 QML)
            [ ] qt5declarative       # QML 声明式 (如不用 QML)
```

### 5.2 Qt Configure 优化参数

如果手动编译 Qt，使用以下 configure 参数：

```bash
./configure \
    -prefix /usr \
    -hostprefix /opt/qt-host \
    -release \                       # Release 模式
    -strip \                         # Strip 符号
    -optimize-size \                 # 优化体积而非速度
    -ltcg \                          # 链接时优化 (Link Time Code Generation)
    -silent \
    -opensource -confirm-license \
    -platform linux-g++ \
    -device linux-rockchip-g++ \
    -no-opengl \                     # ★ 无 GPU
    -no-opengles2 \
    -no-eglfs \                      # ★ 无 EGLFS
    -linuxfb \                       # ★ 仅保留 LinuxFB (如有屏)
    -no-xcb \                        # ★ 无 X11
    -no-directfb \
    -no-gbm \
    -no-kms \
    -no-cups \                       # 无打印
    -no-dbus \                       # 如不用 D-Bus
    -no-glib \                       # 如不用 GLib
    -no-gtk \
    -no-openssl \                    # 如不需要 SSL (如需要保留)
    -no-icu \                        # ★ 去掉 ICU (节省 20MB+)
    -no-iconv \                      # 如不需要字符编码转换
    -no-harfbuzz \                   # ★ 去掉 HarfBuzz (如不需要复杂文字排版)
    -no-freetype \                   # 如使用 Qt 自带 freetype
    -no-gif \                        # 如不需要 GIF
    -no-libjpeg \                    # 如不需要 JPEG
    -no-libpng \                     # 如不需要 PNG (UI 可能需要保留)
    -no-feature-movie \
    -no-feature-printer \
    -no-feature-printdialog \
    -no-feature-pdf \
    -no-feature-whatsthis \
    -no-feature-wizard \
    -no-feature-texthtmlparser \     # 如不需要 HTML 解析
    -nomake examples \               # ★ 不编译示例
    -nomake tests \                  # ★ 不编译测试
    -nomake tools \                  # 不编译工具
    -skip qtwebengine \
    -skip qtmultimedia \
    -skip qtlocation \
    -skip qtsensors \
    -skip qtconnectivity \           # Bluetooth + NFC
    -skip qt3d \
    -skip qtdatavis3d \
    -skip qtcharts \
    -skip qtgamepad \
    -skip qtpurchasing \
    -skip qtscript \
    -skip qtspeech \
    -skip qtvirtualkeyboard \
    -skip qtwayland \
    -skip qtwebchannel \
    -skip qtwebsockets \
    -skip qtwebview
```

### 5.3 Qt 库体积参考

| Qt 模块 | 大致体积 (stripped) | 网关是否需要 |
|---------|-------------------|-------------|
| libQt5Core.so | 3~5 MB | **必须** |
| libQt5Network.so | 1~2 MB | **必须** |
| libQt5SerialPort.so | 100~200 KB | **必须** |
| libQt5SerialBus.so | 200~400 KB | **必须** (CAN) |
| libQt5Gui.so | 3~5 MB | 有屏保留 |
| libQt5Widgets.so | 4~7 MB | 有屏保留 |
| libQt5Sql.so | 200~500 KB | 可选 (日志存储) |
| libQt5Qml.so | 3~5 MB | 不用QML则去掉 |
| libQt5Quick.so | 4~6 MB | 不用QML则去掉 |
| libQt5WebEngine | 50~100 MB | **必须去掉** |
| libQt5Multimedia | 1~2 MB | **去掉** |
| libQt5Bluetooth | 500 KB | **去掉** |
| ICU 库 (libicu*) | 20~30 MB | **强烈建议去掉** |

**裁剪后 Qt 最小占用约 5~15 MB (无屏) 或 15~30 MB (有屏+Widgets)。**

---

## 6. U-Boot 裁剪

U-Boot 裁剪收益相对较小，但对启动速度有帮助：

```
# U-Boot menuconfig
make rk3506_defconfig
make menuconfig

# 可关闭项:
Command line interface --->
    [ ] bootd        # 如不用
    [ ] iminfo
    [ ] USB commands
    Boot commands --->
        [ ] bootm     # 如不用 uImage
Device Drivers --->
    [ ] USB support   # 如不从 USB 启动
    [ ] Sound support
    [ ] Video support # 如无屏
    [ ] WiFi support
```

---

## 7. 分区规划建议

### 7.1 SPI NAND 256MB 分区 (推荐方案)

```
┌──────────────────────────────────────────────────┐
│ 256MB SPI NAND 分区布局                           │
├─────────────┬──────────┬────────────────────────┤
│ 分区名       │ 大小     │ 说明                    │
├─────────────┼──────────┼────────────────────────┤
│ loader      │ 1 MB     │ SPL + U-Boot            │
│ uboot-env   │ 512 KB   │ U-Boot 环境变量          │
│ kernel      │ 5~8 MB   │ 裁剪后内核 (zImage+DTB) │
│ rootfs      │ 60~80 MB │ SquashFS (只读根文件系统) │
│ app         │ 80~100 MB│ 网关应用 + Qt 库         │
│ data        │ 50~80 MB │ 可写分区 (日志、配置等)   │
│ reserved    │ 剩余     │ 坏块预留 + OOB            │
└─────────────┴──────────┴────────────────────────┘
```

### 7.2 eMMC 256MB 分区

```
┌──────────────────────────────────────────────────┐
│ 256MB eMMC 分区布局                               │
├─────────────┬──────────┬────────────────────────┤
│ 分区名       │ 大小     │ 说明                    │
├─────────────┼──────────┼────────────────────────┤
│ loader      │ 4 MB     │ SPL + U-Boot            │
│ kernel      │ 8 MB     │ 裁剪后内核               │
│ rootfs      │ 80 MB    │ ext4 根文件系统           │
│ userdata    │ ~160 MB  │ 应用 + 数据              │
└─────────────┴──────────┴────────────────────────┘
```

### 7.3 使用 SquashFS 压缩根文件系统

SquashFS 压缩率高 (约50%)，适合 Flash 空间有限的场景：

```bash
# 制作 SquashFS 镜像
mksquashfs rootfs/ rootfs.squashfs -comp xz -b 256K -Xbcj arm

# 内核需开启:
File systems --->
    [*] SquashFS 4.0 - Squashed file system support
    [*]   Use XZ decompression
```

---

## 8. 裁剪后验证清单

裁剪完成后逐项验证：

### 8.1 功能验证

| 验证项 | 验证命令 | 预期结果 |
|--------|---------|---------|
| CAN 收发 | `candump can0` / `cansend can0 123#DEADBEEF` | 正常收发 |
| RS485 通信 | `echo "test" > /dev/ttyS1` | 正常收发 |
| 以太网连通 | `ping 192.168.1.1` | 延迟正常 |
| 网关应用启动 | 运行 Qt 程序 | 正常启动 |
| 看门狗 | `echo V > /dev/watchdog` | WDT 正常 |
| GPIO | `echo 1 > /sys/class/gpio/gpioN/value` | LED 正常 |

### 8.2 空间占用目标

| 项目 | 裁剪前 (预估) | 裁剪后目标 |
|------|-------------|-----------|
| 内核 (zImage) | 8~12 MB | 3~5 MB |
| 内核模块 | 10~30 MB | 0~2 MB (尽量编入内核) |
| Rootfs (未压缩) | 200~500 MB | 40~80 MB |
| Rootfs (SquashFS XZ) | — | 20~40 MB |
| Qt 库 | 50~100 MB | 5~15 MB (无屏) / 15~30 MB (有屏) |
| 总占用 | — | **40~80 MB** |

### 8.3 内存占用目标

256MB DDR 分配参考：

| 项目 | 预估占用 |
|------|---------|
| 内核 + 基础系统 | 20~30 MB |
| Qt 基础库 (共享内存) | 10~20 MB |
| 网关应用程序 | 10~30 MB |
| CAN/串口缓冲区 | 5~10 MB |
| 网络缓冲区 | 5~10 MB |
| **剩余可用** | **~150 MB+** |

---

## 附录 A: 常用裁剪命令速查

```bash
# 查看内核配置中已开启的选项数量
grep "=y" .config | wc -l
grep "=m" .config | wc -l

# 查看内核大小
ls -lh arch/arm/boot/zImage

# 查看 rootfs 大小及各目录占用
du -sh output/target/*/ | sort -rh

# 查看 Qt 库大小
ls -lh output/target/usr/lib/libQt5*.so*

# 查看运行时内存占用
free -m
cat /proc/meminfo

# 查看哪些内核模块被加载
lsmod

# 查看系统启动耗时
systemd-analyze  # 如用 systemd
dmesg | tail     # 查看启动日志
```

## 附录 B: 裁剪优先级矩阵

按节省空间大小排序的裁剪优先级：

| 优先级 | 裁剪项 | 节省空间 (rootfs) | 节省空间 (内核) | 复杂度 |
|--------|--------|------------------|----------------|--------|
| P0 | 去掉 Qt WebEngine | 50~100 MB | — | 低 |
| P0 | 去掉 ICU 库 | 20~30 MB | — | 低 |
| P0 | 去掉 GPU 库 (Mali) | 5~20 MB | 1~5 MB | 低 |
| P0 | 去掉 WiFi 固件+工具 | 2~10 MB | 1~3 MB | 低 |
| P1 | 去掉蓝牙工具栈 | 1~5 MB | 500 KB | 低 |
| P1 | 去掉音频 (ALSA/Pulse) | 1~5 MB | 500 KB~2 MB | 低 |
| P1 | 去掉多媒体 (GStreamer/FFmpeg) | 5~15 MB | 1~3 MB | 低 |
| P1 | 去掉 Python/Node 等 | 10~50 MB | — | 低 |
| P2 | Qt 模块精简 | 10~30 MB | — | 中 |
| P2 | 使用 SquashFS | 节省 50%+ | — | 中 |
| P2 | 去掉不需要的文件系统 | — | 200~500 KB | 低 |
| P3 | Strip + -Os 优化 | 10~30% | 10~20% | 低 |
| P3 | 去掉文档/locale/字体 | 5~20 MB | — | 低 |
| P3 | BusyBox 替代 bash/coreutils | 1~5 MB | — | 低 |

---

**总结**：对于 256MB RAM + 256MB Flash 的 RK3506 网关场景，通过以上裁剪，可将系统总占用控制在 **40~80MB** Flash 空间以内，运行时内存占用在 **50~80MB** 以内，为网关应用留出充足的存储和运行空间。
